# CLAUDE.md

Claude Code がこのリポジトリで作業する際のガイドです。

## プロジェクト概要

最大7人対戦のオンライン大富豪Webアプリ（Node.js + Express + Socket.IO、ビルド不要のバニラJSクライアント）。
ルール仕様の正は [`大富豪_要件仕様書_v1.1.md`](大富豪_要件仕様書_v1.1.md)。実装の内部設計は [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)、通信仕様は [`docs/SOCKET_API.md`](docs/SOCKET_API.md) を参照。ルール周りの変更は必ずこの3つのドキュメントとの整合を確認すること。

## 起動・開発コマンド

```bash
npm install
npm start        # node server/index.js を起動（npm run dev も同一）
```

- ビルドステップなし。`public/` は静的配信されるバニラJSで、変更は保存して再読み込みするだけで反映される。
- サーバー側（`server/`）を変更した場合はプロセスの再起動が必要（ホットリロードなし）。
- `npm test` に相当するテストスイートは**現状リポジトリに存在しない**。README記載の「動作確認」は開発時の一時スクリプトによるもので、コミットされていない。ロジックを変更した場合は自分で簡易な動作確認（後述）を行うこと。

## アーキテクチャの要点

- **状態はすべてサーバー権威**: ゲームロジック・判定は `server/game/Game.js` に一元化されている。`public/main.js` はサーバーから届く状態を描画し、操作をイベント送信するだけの薄いビュー層（勝敗判定やルール判定を絶対にクライアント側に書かないこと）。
- モジュール構成・ゲーム状態機械・特殊ルールの処理順序は [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) に詳しい。特に「1手のプレイ処理の流れ」は仕様書13章の優先順位（縛り→革命→階段革命→8切り→Jバック→Qボンバー→7わたし→上がり判定→反則判定→ターン進行）と対応しているので、`Game.js#playCards` を触るときは必ず参照すること。
- Socket.IOのイベント名・ペイロード形状を変更する場合は `server/index.js` と `public/main.js` の両方を揃えて直し、[`docs/SOCKET_API.md`](docs/SOCKET_API.md) も更新する。

## コードの規約・設計上の慣習

- CommonJS（`require`/`module.exports`）、`'use strict'` を各ファイル先頭に付与。ESMやTypeScriptへの移行はしない。
- インデントは2スペース。既存コードは基本的にセミコロンあり。
- ログメッセージ・UI文言・コメントはすべて日本語。新規追加分もこのトーンに合わせる。
- ランクは3〜15の数値表現（3=3, …, J=11, Q=12, K=13, A=14, 2=15）、ジョーカーは `rank:16` の番兵値。この設計により「通常時の強さ＝数値昇順」「革命時は `18 - rank`」という単純な式で強弱が計算できている。新しいカード関連ロジックを書くときはこの表現を崩さないこと（詳細は `server/game/cards.js`）。
- `Game` は `EventEmitter`。状態が変わるたびに `update`、終了時に `gameEnd` を発火し、`server/index.js` 側がそれを購読して全プレイヤーへブロードキャストする。`Game.js` 内で新しい状態変化を追加したら、対応箇所で `this.emit('update')` を呼び忘れないこと（呼ばないとクライアントに反映されない）。
- 個人情報（手札）は `getPublicState(viewerId)` で呼び出し元ごとに異なる内容を返す。全員に同じ状態をブロードキャストしないよう `server/index.js#broadcastGame` はプレイヤーごとに個別送信している。この非対称性を壊さないこと。

## 変更時に気をつけること

- ルール変更・追加は必ず [`大富豪_要件仕様書_v1.1.md`](大富豪_要件仕様書_v1.1.md) と突き合わせる。仕様書に無い拡張（例: 10捨て）を追加・変更する場合は README・`docs/ARCHITECTURE.md` の該当箇所も更新する。
- `Game.js` はタイマー（`turnTimer` / `actionTimer`）を内部で持つステートフルなクラス。テストやデバッグでインスタンスを使い捨てる場合は `destroy()` を呼んでタイマーを解放すること（呼ばないとNodeプロセスが終了しない・タイマーが積み残る）。
- 手動での動作確認手順（テストスイートが無いため）:
  1. `npm start` でサーバーを起動し、複数のブラウザタブ（または別ブラウザ）から `http://localhost:3000` に接続してルーム作成〜参加〜開始までを一通り確認する。
  2. ロジックのみを変更した場合は、`server/game/Game.js` を直接 `require` してランダムなカードプレイを繰り返すスクリプトを一時的に書き、例外やフリーズが起きないか確認するのが手早い（コミットは不要、スクラッチファイルとして実行して削除する）。
- Windows環境（PowerShell/Git Bash併用）が前提。パス操作やシェルコマンドはOS非依存なNode.js標準APIを優先する。
